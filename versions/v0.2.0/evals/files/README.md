# Evaluation Fixtures

Add licensed or internally created reference screenshots matching the filenames in `../evals.json` before running the visual evaluations. The package intentionally does not distribute third-party UI screenshots.
